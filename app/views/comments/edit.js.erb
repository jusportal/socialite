$('#common-modal').modal('show');
$(".modal-title").text("Edit comment");
$(".modal-body").html("<%= escape_javascript(render partial: 'shared/comments/form', locals: {post: @post, comment: @comment}) %>");
